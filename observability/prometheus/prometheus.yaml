global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # The application's metrics, republished by the collector with the resource
  # attributes as labels.
  - job_name: application
    honor_labels: true
    static_configs:
      - targets: ["otel-collector:8889"]

  # The collector's own health: what it received, dropped, and failed to send.
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8888"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
